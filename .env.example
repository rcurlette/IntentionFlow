# FlowTracker Environment Variables
# Copy this file to .env.local and fill in your values

# Supabase Configuration (for future auth & sync features)
# VITE_SUPABASE_URL=your_supabase_project_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google OAuth (for future authentication)
# VITE_GOOGLE_CLIENT_ID=your_google_client_id

# Optional: Analytics or other integrations
# VITE_ANALYTICS_ID=your_analytics_id

# For local development, no environment variables are required
# The app uses localStorage for data persistence
